namespace Frontend;

/// <summary>
/// i18n リソースのマーカークラス。
/// Resources/SharedResources.ja.resx / Resources/SharedResources.en.resx と対応する。
/// ResourcesPath = "Resources" の場合、マーカークラスの名前空間はルート名前空間と一致させる必要がある。
/// （Frontend.Resources にすると Resources/Resources/SharedResources.*.resx を探してしまうため）
/// </summary>
public class SharedResources;
